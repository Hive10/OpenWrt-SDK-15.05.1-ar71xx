menuconfig IMAGEOPT
	bool "Image configuration"
	default n
source "package/*/image-config.in"
menu "Extra packages"

	config PACKAGE_tang
		tristate "tang.. tang v4 - daemon for binding data to the presence of a third party"
		default y if DEFAULT_tang
		default m if ALL
		select PACKAGE_librt if USE_GLIBC
		select PACKAGE_xinetd
		select PACKAGE_libhttp_parser
		select PACKAGE_libc
		select PACKAGE_jose
		select PACKAGE_libssp if SSP_SUPPORT
		select PACKAGE_libpthread if USE_GLIBC
		help
		 Tang is a small daemon for binding data to the presence of a third party.
		 https://github.com/latchset/tang
		 

endmenu

